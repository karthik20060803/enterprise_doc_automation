services:
  - type: web
    name: enterprise-doc-automation
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install gunicorn && pip install -r requirements-render.txt
    startCommand: gunicorn web_frontend:app
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: IDP_FRONTEND_DEBUG
        value: "false"
