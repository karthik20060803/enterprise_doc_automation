services:
  - type: web
    name: enterprise-doc-automation
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install gunicorn==21.2.0 && pip install -r requirements-render.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT web_frontend:app
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: IDP_FRONTEND_DEBUG
        value: "false"
